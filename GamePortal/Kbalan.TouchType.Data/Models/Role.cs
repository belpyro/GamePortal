namespace Kbalan.TouchType.Data.Models
{
    /// <summary>
    /// Enum for TouchTypeGame registered visitors roles(user = 0 , premium user =1 and admin = 2)
    /// </summary>
    public enum Role
    {
        User,
        PremiumUser, 
        Admin
    }
}
